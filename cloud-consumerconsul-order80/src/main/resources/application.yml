server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 120.79.92.154
      port: 8500
      discovery:
        service-name: ${spring.application.name}



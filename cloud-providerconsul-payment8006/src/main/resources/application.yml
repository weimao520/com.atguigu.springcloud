server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: 120.79.92.154
      port: 8500
      discovery:
        service-name: ${spring.application.name}

